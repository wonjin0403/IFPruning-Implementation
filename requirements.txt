torch>=2.4.0
transformers==4.48.0
accelerate>=0.34.0
deepspeed>=0.15.0
datasets>=2.20.0
huggingface_hub>=0.24.0
tokenizers>=0.20.0
sentencepiece
safetensors

hydra-core>=1.3.2
omegaconf>=2.3.0

tqdm
numpy
pandas
einops
natsort

# Evaluation
lm-eval>=0.4.4

# Logging / monitoring
wandb
tensorboard

# Optional: faster attention
# flash-attn  # install separately with --no-build-isolation
